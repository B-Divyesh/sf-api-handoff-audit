[
  {
    "id": "repo-gaps",
    "claim": "Finds undocumented variables and setup gaps before handoff",
    "where": "landing page and README",
    "test": "npm run test:e2e -- --grep @claim:repo-gaps",
    "sandbox": "fresh temporary repository with an undocumented variable and no setup_steps"
  },
  {
    "id": "absent-fixtures",
    "claim": "Finds absent fixtures before handoff",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:absent-fixtures",
    "sandbox": "fresh temporary repository with a configured fixture path that does not exist"
  },
  {
    "id": "workspace-formats",
    "claim": "Scans Bruno, Postman, and .http files for {{NAME}}, ${NAME}, and $dotenv NAME variable references",
    "where": "landing page and README",
    "test": "npm run test:e2e -- --grep @claim:workspace-formats",
    "sandbox": "fresh temporary repository with one file of each supported type"
  },
  {
    "id": "local-free-audit",
    "claim": "The free audit runs locally without an account, telemetry, or a network request",
    "where": "landing facts, privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:local-free-audit",
    "sandbox": "fresh temporary repository with HTTP and HTTPS proxies pointed at a loopback request counter"
  },
  {
    "id": "env-file",
    "claim": "Reads NAME=value values from a chosen --env-file and reports their names and states without their values",
    "where": "privacy page and README",
    "test": "npm run test:e2e -- --grep @claim:env-file",
    "sandbox": "fresh temporary repository and environment file with a sentinel secret"
  },
  {
    "id": "redacted-reports",
    "claim": "Terminal, JSON, and HTML reports hide supplied variable values and response bodies",
    "where": "landing facts, product preview, and README",
    "test": "npm run test:e2e -- --grep @claim:redacted-reports",
    "sandbox": "fresh temporary repository with a known sentinel secret and a loopback response-body sentinel"
  },
  {
    "id": "explicit-smoke",
    "claim": "Runs only the named smoke request against the selected configured target and does not follow redirects",
    "where": "how it works, privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:explicit-smoke",
    "sandbox": "two fresh loopback HTTP servers and a temporary repository"
  },
  {
    "id": "target-policy",
    "claim": "Accepts HTTP or HTTPS local targets and requires HTTPS staging targets",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:target-policy",
    "sandbox": "fresh temporary repositories with local and staging target configurations"
  },
  {
    "id": "exit-codes",
    "claim": "Uses exit code 0 for pass, 1 for audit findings or a failed smoke response, and 2 for invalid input or configuration; --json writes one JSON report to stdout",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:exit-codes",
    "sandbox": "fresh temporary repositories for passing, finding, and invalid-input CLI runs"
  },
  {
    "id": "cli-demo-isolation",
    "claim": "The CLI demo copies bundled sample data to a new temporary directory, writes an HTML report there, prints its path, and does not write to your repository",
    "where": "README and demo documentation",
    "test": "npm run test:e2e -- --grep @claim:cli-demo-isolation",
    "sandbox": "fresh temporary working repository and the bundled Parcel Lane sample"
  },
  {
    "id": "package-install",
    "claim": "The ready-to-publish package installs one api-handoff-audit binary with --help, version 0.1.0, and demo --json support",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:package-install",
    "sandbox": "fresh extracted .crate and isolated cargo install root"
  },
  {
    "id": "build-artifacts",
    "claim": "npm run build creates the release binary and deployable site in dist/site/",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:build-artifacts",
    "sandbox": "clean repository checkout; assert target/release/api-handoff-audit and dist/site/index.html after the documented build command"
  },
  {
    "id": "sample-report-content",
    "claim": "The bundled sample report has 3 files, 2 setup steps, 1 fixture, 2 smoke requests, and one WAREHOUSE_ID finding",
    "where": "landing preview and browser demo",
    "test": "npm run test:e2e -- --grep @claim:sample-report-content",
    "sandbox": "fresh CLI demo and fresh browser context at /demo?demo=1; compare sample counts, finding, file, and next step"
  },
  {
    "id": "demo-sandbox",
    "claim": "Demo changes are not saved and the demo sends no third-party requests",
    "where": "demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context on /demo with request interception"
  }
]
